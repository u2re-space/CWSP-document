/**
 * Canonical shell/view types — single source (`subsystem/boot/types`).
 * Resolves `shells/types` imports for CrossWord boot + subsystem boot modules.
 */
export * from "../../../../../modules/projects/subsystem/src/boot/types";
