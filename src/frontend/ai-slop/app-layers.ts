/**
 * @deprecated Import from `shared/routing/app-layers` — implementation moved there
 * so boot paths avoid an extra dev fetch under `frontend/ai-slop/`.
 */
export { ensureAppLayers, type AppLayers } from "../../shared/routing/app-layers";
